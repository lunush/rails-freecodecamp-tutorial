<div class="card">
  <div class="card-body">
    <h1>
      <%= @friend.first_name %>
      <%= @friend.last_name %>
      <% if @friend.likes_icecream %>
        <span class="badge bg-secondary">🍨</span>
      <% end %>
    </h1>

    <p class="lead">
      <%= @friend.email %>
      <%= @friend.phone %>
    </p>

    <p>
      <strong>Birthday:</strong>
      <%= @friend.birthday %>
    </p>

    <%= link_to 'Edit', edit_friend_path(@friend), class:"btn btn-primary" %>
    <%= link_to 'Back', friends_path, class:"btn btn-primary" %>
  </div>
</div>
